import styled from '@emotion/styled';

const NoticeCard = () => {
  return <S.Card>NoticeCard</S.Card>;
};

export default NoticeCard;

const S = {
  Card: styled.div`
    background-color: blue;
    border-radius: 24px;
    height: 100%;
    width: 70%;
  `,
};
